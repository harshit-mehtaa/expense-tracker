{
  "//": [
    "Template for ~/.pi/agent/models.json — pi reads models.json from its own config dir,",
    "NOT from the repo, so copy this there rather than pointing pi at this file:",
    "",
    "    mkdir -p ~/.pi/agent && cp .pi/models.example.json ~/.pi/agent/models.json",
    "",
    "Sized for a laptop RTX 5070 Ti (12 GB VRAM). qwen3.5:9b is 6.6 GB at q4_K_M with a",
    "256K native context; its hybrid DeltaNet/Attention stack means long context costs far",
    "less memory than on a dense transformer. See .claude/memory/cost-routing.md.",
    "",
    "Setup:",
    "    ollama pull qwen3.5:9b",
    "    export OLLAMA_MAX_LOADED_MODELS=1",
    "",
    "CONTEXT LENGTH. Ollama does not serve the native 256K by default, and pi reaches it",
    "over the OpenAI-compatible API, which has no field to request a context size. Setting",
    "it per-model here would NOT work. Bake it into a derived tag instead:",
    "",
    "    printf 'FROM qwen3.5:9b\\nPARAMETER num_ctx 65536\\n' > Modelfile",
    "    ollama create qwen3.5:9b-64k -f Modelfile",
    "",
    "then use qwen3.5:9b-64k below and in the pi row of the tier map. Verify with",
    "`ollama ps` — if the reported size nears 12 GB, lower num_ctx before it spills to",
    "system RAM and throughput collapses."
  ],
  "providers": {
    "ollama": {
      "baseUrl": "http://localhost:11434/v1",
      "api": "openai-completions",
      "apiKey": "ollama",
      "compat": {
        "supportsDeveloperRole": false,
        "supportsReasoningEffort": false
      },
      "models": [
        {
          "id": "qwen3.5:9b-64k",
          "name": "Qwen3.5 9B @ 64k (all tiers)"
        },
        {
          "id": "qwen3.5:9b",
          "name": "Qwen3.5 9B (stock context — see note above)"
        },
        {
          "id": "qwen3.5:4b",
          "name": "Qwen3.5 4B (optional cheaper fast tier)"
        }
      ]
    }
  }
}
